# Backend server settings
RELAY_API_SECRET=replace-with-at-least-32-random-characters
RELAY_API_PORT=4317
RELAY_API_HOST=127.0.0.1
# Set this only when browser clients need access. The desktop app does not need CORS.
# RELAY_API_ORIGIN=http://localhost:5173
